#include<stdio.h>
main()
{
	char c[20];
	int i;
 	printf("enter the sentence:");
	 scanf("%s",&c[i]);
	 for(i=0;i<20;i++){
	 	if(i%2==0){
	 		c[i+1]==c[i+1]+32;
	 	 printf("%s",c[i]);
		 }
		 else 
		 c[i]=c[i];
		
	 	
	 }
	  for(i=0;i<20;i++){
	 	if(c[i]==' '){
	 		c[i+1]=c[i+1]+32;
	 		printf("%s",c[i]);
		 }
		 
	 	
	 }
	 
	
}
